import pandas as pd

if st.button("📥 Generar Excel"):
    historial = leer_historial()

    df = pd.DataFrame(historial, columns=["N1","N2","N3","N4","N5","N6"])

    st.download_button(
        label="⬇️ Descargar Excel",
        data=df.to_csv(index=False),
        file_name="melate_resultados.csv",
        mime="text/csv"
    )
